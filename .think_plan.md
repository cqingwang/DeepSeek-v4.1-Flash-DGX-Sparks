# DeepSeek-V4.1 Flash TP4 合并与部署计划

## 范围与 SSOT

- 目标模块：`spark/deepseek_v4.1_flash_switchless/`。
- 本地 `HEAD=6ee12fa` 的四节点 TP4/EP2 switchless-ring 配方是运行时 SSOT；本轮合并输入为 `knapcio/main=07c3b0c`，只吸收其新配方代码、适配器、测试与文档，不替换本地环网拓扑和传输控制面。
- 远程启动必须经外层 `spark/deploy.sh`，链路为 `config.yaml -> program.py -> managed .env -> start-tp4.sh -> start.sh -> Docker`。
- 权重保持各节点宿主机已有目录，只读挂载到 `/models/DeepSeek-V4.1-Flash:ro`；禁止复制、下载或修改远程权重。

## 冲突决策

- `.env.example`、`.env.tp4.example`、`README.md`、`Dockerfile`、`boot.py` 和 `start.sh` 的生产配置以本地 TP4/ring 语义为主。
- `adapter/sitecustomize.py` 并集保留本地 KV/自适应 chunk/indexer 适配，同时接入上游编码、loop-abort、验证上限、FP8 twin、draft 和 autotune 钩子；所有新增钩子继续由环境变量门控。
- `files/nccl.sh` 作为通用 ring 预检和安全挂载实现保留；启动器同时保留本地兼容 shim/PEER_HCA/EXTRA_DOCKER_ENV 注入。

## 验证门禁

1. 无冲突索引、无冲突标记、`git diff --check`。
2. `bash -n` 覆盖 `start.sh`、`start-tp4.sh`、`stop.sh`、`files/*.sh`。
3. Python 编译及现有/新增离线回归测试，重点检查 TP4 默认、ring NCCL、模型只读挂载、boot smoke 和适配器导入。
4. 外层 `spark/deploy.sh start --target deepseek_v4.1_flash_switchless --model deepseek-ai/DeepSeek-V4.1-Flash` 部署；不得直接执行嵌套启动脚本作为部署证据。
5. 远端验收必须同时证明四 rank/world size、实际 Docker 命令、只读模型挂载、`/health`、`/v1/models`、真实 Chat、NCCL `NET/IB` 初始化与 RDMA 计数变化；仅 `Up` 不算成功。

## 已知边界

- 本机若无 NVIDIA Docker/GPU 或远程当前 GPU 被其他配方占用，只完成静态与只读远端预检并报告阻断，不强行停用无关服务。
- 性能数字不因本次合并自动重测；只有同一 DecodeBench 请求口径下的实测才能改变既有 TP4 结论。

## 本轮合并后的最终配方

- 镜像和新配方：使用合并后的 `Dockerfile.canary-roce` 及其 pinned base image，启用 fast-load、chunked indexer、cache report、prefill-SP、L2 prefetch、HC fused、FP8 twin、draft-head FP8、block verify、replicated split 等门控能力。
- 环网不变：`config.yaml` 的 `spark-a -> spark-c -> spark-d -> spark-b` rank 顺序、`NCCL_ALGO=RING`、`NCCL_NET=IB`、GID 强制 `-1`、`/opt/nccl-ringonly`、`LD_PRELOAD` ring shim、逐 rank `PEER_HCA` 和 `NFS_SHARE=0` 均保留。
- RoCE 新配方明确关闭：`SGLANG_ROCE_ALLREDUCE=0`、`DSV41_ROCE_GATHER=0`。因此新 README 中 switched-RoCE 的吞吐数字不能直接冒充本地物理环网结果。
- 参数迁移：最终 managed env 使用新配方的 `DSV41_CACHE_GIB=4`、静态显存比例 `0.80`、最大并发 `16`、chunked prefill `4096`，但仍由既有 `spark/deploy.sh` 写入 `.env.tp4`，不增加新的部署入口。
- 远端流程：本地 `spark/` 是唯一源，先 rsync 到四台机器的 `/opt/spark/`，再在 `spark-a` 执行原有 `./deploy.sh start ...`；模型仍使用各节点 `/opt/models` 的只读挂载。

## 本轮验证口径

- 合并前后先执行冲突扫描、shell/Python/YAML 静态检查和原有离线回归测试，再提交嵌套 merge commit 及外层配置 glue。
- 部署后验证四节点 rank/world size、实际 NCCL `NET/IB`、ring-only library/shim、只读模型挂载、`/health`、`/v1/models`、Chat 和新增配方日志。
- 速度使用仓库现有 sparkDash/DecodeBench 脚本，固定思考关闭、温度和输出长度，并区分三种基线：本地环网旧 README、新配方 switched-RoCE README、合并后本地环网实测。只有同一传输和请求口径的环网基线用于判定环网是否达标。
